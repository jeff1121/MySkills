# FinMind MCP Server — 建置映像檔
# 使用方式：docker compose -f docker-compose.build.yml build

services:
  finmind-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: jeffhou/finmind-mcp-server:latest
    platform: linux/amd64
